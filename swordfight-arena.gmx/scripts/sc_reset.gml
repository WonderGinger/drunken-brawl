//RESET PLAYERS
with(o_player_1) {
    x = x_init;
    y = y_init;
    st_player = ps.idle;
    img_spd = .12;
}
with(o_player_2) {
    x = x_init;
    y = y_init;
    st_player = ps.idle;
    img_spd = .12;
}

